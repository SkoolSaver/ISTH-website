/**
 * Services Module
 * Business logic layer for API interactions
 *
 * Services encapsulate business logic and use the API client
 * to interact with backend endpoints.
 *
 * Example:
 * ```ts
 * import { contactService } from '@/lib/services'
 *
 * const result = await contactService.submitForm(formData)
 * ```
 */

// Export services here as they are created
// Example: export { contactService } from './contact'
